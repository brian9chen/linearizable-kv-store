LLM USAGE:
Part 2—Reliable Lock:
We used Cursor tab some when writing the funcionality. We ended up turning it off after it kept suggesting full blocks of code (likely because this lab is already published online and in its training set), because we wanted the solution to be our own work. 

Part 4-Unreliable Lock: 
We used Claude to help brainstorm and refine  lock.go Part 4 implementation. We reviewed, tested, and verified the final code ourselves to ensure it matches the assignment requirements.

Extension:
With permission, we used Claude Code set to Opus 4.7 to complete the base Raft lab used for the extension, writing raft.go. This was done by feeding Opus our repo + the website for MIT lab 3 (https://pdos.csail.mit.edu/6.824/labs/lab-raft1.html), and then instructing it to copy over tests to make sure everything worked properly from another repo because we cloned from an outdated version of the MIT lab originally. We had Claude Opus update tests for kvsrv1 and kvsrv1/lock, as well as the given server.go and test.go files in order to make sure the kvstore code still worked with the new base testing environment (it does), the changes to server.go and test.go were minor and just to make sure syntax/api usage matched. The full list of changed files so that the extension could work is on commit c2afd84567600673ff56f975dea763ad1bbcc156, the vast majority of which were just cut/paste from the updated repo by Opus 4.7. If you want to check the before/after for Opus 4.7 of whether tests pass, that is the commit to look at. The full Opus 4.7 chat can be sent upon request if necessary. 
